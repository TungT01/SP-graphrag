# SP-GraphRAG: Structural-entropy Penalized GraphRAG
# 核心依赖

# === 核心依赖 ===
networkx>=3.0
numpy>=1.24.0
pandas>=2.0.0
pyyaml>=6.0

# === NLP / 文本处理 ===
spacy>=3.5.0

# === 图算法 / 社区检测 ===
leidenalg>=0.10.0
python-igraph>=0.11.0

# === 嵌入模型 / ML（baselines 使用）===
sentence-transformers>=2.2.0

# === Web / API（本地嵌入服务器）===
fastapi>=0.100.0
pydantic>=2.0.0
uvicorn>=0.23.0

# === 可选依赖（按需启用）===
# datasets>=2.14.0        # HuggingFace datasets（数据加载）
# graphrag>=0.3.0         # Microsoft GraphRAG（官方基线对比）
# nltk>=3.8.0             # 句子分词（有 fallback）
# pyarrow>=12.0.0         # Parquet 文件支持
# tiktoken>=0.5.0         # OpenAI token 计数（有 fallback）
# rank-bm25>=0.2.2        # BM25 检索
